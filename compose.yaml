services:
  altec-web:
    build:
      context: .
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://www.altec-sz.com}
        NEXT_PUBLIC_DOWNLOADS_CDN_BASE_URL: ${NEXT_PUBLIC_DOWNLOADS_CDN_BASE_URL:-}
    image: altec-web:local
    ports:
      - "3000:80"
